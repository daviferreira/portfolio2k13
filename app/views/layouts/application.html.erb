<!DOCTYPE html>
<html>
<head>
    <title>Portfolio2k13</title>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "pages", :media => "all" %>
    <%= csrf_meta_tags %>
</head>
<body class="<%= params[:controller] %>-<%= params[:action] %>">
    <%= render "shared/notifications" %>
    <%= render "shared/user_info" %>
    <div<% if @has_footer %> class="l-content"<% end %>>
        <div class="top-nav-bar">
            <div class="container clearfix">
                <div class="language">
                    <% if I18n.locale == :en %>
                        <%= link_to raw("ver em português &#8250;"), root_path %>
                    <% else %>
                        <%= link_to raw("switch to english &#8250;"), localized_root_path(:locale => "en") %>
                    <% end %>
                </div>
                <div class="title">
                    <a href="#">
                        <h1>Davi Ferreira</h1>
                        <h2>Code Crafter</h2>
                    </a>
                </div>
                <nav class="clearfix">
                    <%= link_to t('home'), root_path %>
                    <%= link_to t('portfolio'), projects_path %>
                    <%= link_to t('blog'), posts_path %>
                    <a href="#">Book</a>
                    <a href="#">Presentations</a>
                    <a href="#">About</a>
                </nav>
            </div>
        </div>
        <%= yield %>
    </div>
    <% if @has_footer %>
    <footer>
        <div class="container">
            <div class="m-contact">
                <h5>Get in touch</h5>
                <%= mail_to "hi@daviferreira.com" %>
            </div>
            <div class="m-credits">
                <a class="source-code" href="">&raquo; source code</a>
            </div>
        </div>
    </footer>
    <% end %>
    <%= javascript_include_tag "application" %>
</body>
</html>
