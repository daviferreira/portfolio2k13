<!DOCTYPE html>
<html>
<head>
    <title>Portfolio2k13</title>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag "pages", :media => "all" %>
    <%= csrf_meta_tags %>
</head>
<body class="<%= params[:controller] %>-<%= params[:action] %><% if params[:action] != 'index' %> secondary<% end %>">
    <%= render "shared/notifications" %>
    <%= render "shared/user_info" %>
    <div class="l-content">
        <div class="top-nav-bar">
            <div class="container clearfix">
                <div class="language">
                    <% if I18n.locale == :en %>
                        <%= link_to raw("ver em português &#8250;"), root_path %>
                    <% else %>
                        <%= link_to raw("switch to english &#8250;"), localized_root_path(:locale => "en") %>
                    <% end %>
                </div>
                <div class="title">
                    <a href="#">
                        <h1>Davi Ferreira</h1>
                        <h2>Code Crafter</h2>
                    </a>
                </div>
                <nav class="clearfix">
                    <a href="#">Home</a>
                    <a href="#">Portfolio</a>
                    <a href="#">Blog</a>
                    <a href="#">Book</a>
                    <a href="#">Presentations</a>
                    <a href="#">About</a>
                </nav>
            </div>
        </div>
        <%= yield %>
    </div>
    <footer>
        <div class="container">
            <div class="m-contact">
                <h5>Get in touch</h5>
                <%= mail_to "hi@daviferreira.com" %>
            </div>
            <div class="m-credits">
                <a class="source-code" href="">&raquo; source code</a>
            </div>
        </div>
    </footer>
    <%= javascript_include_tag "application" %>
</body>
</html>
