<!DOCTYPE html>
<html>
<head>
    <title>Portfolio2k13</title>
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag params[:controller], :media => "all" %>
    <%= csrf_meta_tags %>
</head>
<body class="<%= params[:controller] %>-<%= params[:action] %>">
    <div<% if @has_footer %> class="l-content"<% end %>>
        <div class="top-nav-bar">
            <div class="container clearfix">
                <div class="language">
                    <% if I18n.locale == :en %>
                        <%= link_to raw("ver em português &#8250;"), root_path %>
                    <% else %>
                        <%= link_to raw("switch to english &#8250;"), localized_root_path(:locale => "en") %>
                    <% end %>
                </div>
                <div class="title">
                    <a href="<%= root_path %>">
                        <h1>Davi Ferreira</h1>
                        <h2>Code Crafter</h2>
                    </a>
                </div>
                <nav class="clearfix">
                    <%= link_to t('home'), root_path %>
                    <%= link_to t('portfolio'), projects_path %>
                    <%= link_to t('blog'), posts_path %>
                    <%= link_to t('book'), page_path('book', :locale => current_locale) %>
                    <%= link_to t('presentations'), page_path('presentations', :locale => current_locale) %>
                    <%= link_to t('about'), page_path('about', :locale => current_locale) %>
                </nav>
            </div>
        </div>
        <%= yield %>
    </div>
    <% if @has_footer %>
    <footer>
        <div class="container">
            <div class="m-contact">
                say <%= mail_to "hi@daviferreira.com" %>
            </div>
        </div>
        <div class="m-credits">
            <a class="source-code" href="">&bullet; source code</a>
        </div>
    </footer>
    <% end %>
    <%= javascript_include_tag "application" %>
</body>
</html>
